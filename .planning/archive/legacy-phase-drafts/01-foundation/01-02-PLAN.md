---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/fars/api/__init__.py
  - src/fars/api/main.py
  - tests/test_topic.py
  - tests/test_normalize.py
  - tests/test_repository.py
  - tests/test_api.py
autonomous: true
requirements: [META-04]
user_setup: []
must_haves:
  truths:
    - Local API exposes health, topic expansion, paper ingestion, and paper read endpoints.
    - API returns graph neighbors and evidence-pack shaped responses for stored data.
    - Tests verify end-to-end behavior locally.
  artifacts:
    - src/fars/api/main.py
    - tests/test_api.py
    - tests/test_repository.py
  key_links:
    - API ingestion endpoint uses repository upsert logic.
    - API graph/evidence responses derive from stored repository state.
---

<objective>
Expose the foundation through a local FastAPI surface and verify it end-to-end.

Purpose: make the knowledge-layer skeleton runnable and testable.
Output: API app, endpoint tests, and local verification suite.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build FastAPI app and dependency wiring</name>
  <files>src/fars/api/__init__.py, src/fars/api/main.py</files>
  <action>Add a local service entrypoint with health, topic expansion, paper ingest/list/read, graph neighbor, and evidence-pack endpoints backed by the repository.</action>
  <verify>python3 -m compileall src</verify>
  <done>API app imports and exposes the expected endpoints.</done>
</task>

<task type="auto">
  <name>Task 2: Add focused unit tests for services and repository</name>
  <files>tests/test_topic.py, tests/test_normalize.py, tests/test_repository.py</files>
  <action>Cover expansion heuristics, identifier normalization, and repository merge/upsert behavior.</action>
  <verify>pytest -q tests/test_topic.py tests/test_normalize.py tests/test_repository.py</verify>
  <done>Core logic is covered by deterministic unit tests.</done>
</task>

<task type="auto">
  <name>Task 3: Add end-to-end API tests</name>
  <files>tests/test_api.py</files>
  <action>Use FastAPI TestClient to verify health, ingest, list, graph, and evidence-pack behavior locally.</action>
  <verify>pytest -q tests/test_api.py</verify>
  <done>Local API behavior is verified end-to-end.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] pytest -q
- [ ] API app imports successfully
- [ ] End-to-end tests pass locally
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- Runnable local knowledge-layer skeleton exists
- Future metadata/parser phases can extend the app without redesigning the core
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
