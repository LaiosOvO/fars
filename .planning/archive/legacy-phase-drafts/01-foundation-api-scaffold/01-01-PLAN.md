---
phase: 01-foundation-api-scaffold
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: ["pyproject.toml", "src/fars/main.py", "src/fars/config.py", "src/fars/api/routes/health.py", "tests/test_health.py"]
autonomous: true
requirements: ["SERV-01", "SERV-02"]
user_setup: []
must_haves:
  truths:
    - API app can be instantiated without external services.
    - Health endpoint returns a successful response with project metadata.
  artifacts:
    - pyproject.toml exists with runnable dependencies.
    - src/fars/main.py exists and exposes the FastAPI app.
    - tests/test_health.py exists and passes.
  key_links:
    - Health route is registered by the app factory.
    - Settings are imported by the app without circular imports.
---

<objective>
Create the Python package scaffold, settings module, app factory, and health endpoint.

Purpose: Establish a runnable service foundation.
Output: FastAPI app, settings, packaging metadata, and health endpoint tests.
</objective>
