---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - src/fars/__init__.py
  - src/fars/config.py
  - src/fars/models.py
  - src/fars/repository.py
  - src/fars/services/topic.py
  - src/fars/services/normalize.py
  - src/fars/connectors/openalex.py
  - src/fars/parsers/base.py
  - src/fars/parsers/grobid.py
autonomous: true
requirements: [META-01, META-02, META-03]
user_setup: []
must_haves:
  truths:
    - Topic expansion produces retrieval-friendly query variants.
    - Paper identifiers normalize into canonical fields.
    - Paper records can be upserted into a canonical internal model.
  artifacts:
    - pyproject.toml
    - src/fars/models.py
    - src/fars/repository.py
    - src/fars/services/topic.py
    - src/fars/services/normalize.py
  key_links:
    - Ingestion models use normalization helpers before persistence.
    - Repository merges records by canonical identifiers when possible.
---

<objective>
Create the Python project scaffold and the canonical domain/repository/services foundation for the FARS knowledge layer.

Purpose: establish the minimal substrate needed for later API, parser, and graph work.
Output: package scaffold, canonical models, normalization helpers, repository abstraction, and metadata/parser adapter interfaces.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@docs/executable-plan.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create project package scaffold and dependencies</name>
  <files>pyproject.toml, src/fars/__init__.py, src/fars/config.py</files>
  <action>Create a runnable Python project with FastAPI/httpx/pydantic/test dependencies and package metadata.</action>
  <verify>python3 -m compileall src</verify>
  <done>Project dependencies and import root are valid.</done>
</task>

<task type="auto">
  <name>Task 2: Implement canonical models and repository</name>
  <files>src/fars/models.py, src/fars/repository.py</files>
  <action>Define canonical paper, version, chunk, edge, and evidence-pack models plus an in-memory repository with upsert/merge behavior.</action>
  <verify>python3 -m compileall src</verify>
  <done>Canonical entities and repository abstraction exist and are usable from tests.</done>
</task>

<task type="auto">
  <name>Task 3: Implement topic expansion and identifier normalization services</name>
  <files>src/fars/services/topic.py, src/fars/services/normalize.py</files>
  <action>Provide deterministic topic expansion and identifier normalization helpers for DOI/arXiv/OpenAlex/Semantic Scholar style identifiers.</action>
  <verify>python3 -m compileall src</verify>
  <done>Helpers can be used by ingestion logic and later API endpoints.</done>
</task>

<task type="auto">
  <name>Task 4: Add metadata and parser adapter interfaces</name>
  <files>src/fars/connectors/openalex.py, src/fars/parsers/base.py, src/fars/parsers/grobid.py</files>
  <action>Create stable client/adapter interfaces without making live external services required for local testing.</action>
  <verify>python3 -m compileall src</verify>
  <done>Adapter layer is present and future phases can extend it without breaking core models.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] python3 -m compileall src
- [ ] Domain models import cleanly
- [ ] Repository can upsert a normalized paper object
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No syntax or import errors introduced
- Domain/repository/service layer is ready for API exposure
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
